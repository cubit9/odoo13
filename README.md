# Install Odoo13 in a Docker Container

- You can use multiple docker-compose files to start at ones:

    ```bash
    sudo docker-compose -f docker-compose.yml -f docker-compose-prod.yml up -d
    ```


